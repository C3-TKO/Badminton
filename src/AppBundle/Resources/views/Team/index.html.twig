{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}
    <h1>Team list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idplayera</th>
                <th>Idplayerb</th>
                <th>Name</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('crud_team__show', { 'id': entity.id }) }}">{{ entity.idPlayerA }}</a></td>
                <td>{{ entity.idPlayerB }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('crud_team__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('crud_team__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('crud_team__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
