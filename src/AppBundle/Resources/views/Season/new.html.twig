{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Season creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('crud_season_') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
