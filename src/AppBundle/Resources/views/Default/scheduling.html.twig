{% extends 'AppBundle::layout.html.twig' %}

{% block body %}
    <section class="main-block">
        <article>
            <section>
                <h1>Spielplanerstellung</h1>
                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </section>
        </article>
    </section>
{% endblock %}