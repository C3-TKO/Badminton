{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}
    <h1>Round list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idseason</th>
                <th>Date</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('crud_round__show', { 'id': entity.id }) }}">{{ entity.idSeason }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('crud_round__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('crud_round__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('crud_round__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
